namespace SchoolProject.ETL.Model.Enums
{
    public enum Datatyp
    {
        Unknown = 0,
        Integer = 10,
        String = 20,
        Boolean = 30,
        EMail = 31,
        Date = 32,
    }
}